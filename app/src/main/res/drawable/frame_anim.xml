<?xml version="1.0" encoding="utf-8"?>
<animation-list xmlns:android="http://schemas.android.com/apk/res/android"
                android:oneshot="false" >

    <!-- animation-list 帧动画 -->
    <!-- android:oneshot的值为 false代表播放多次，true代表只播放一次 -->
    <!-- duration代表每张图片的播放时间 ,定义一个持续时间为50毫秒的动画帧 -->
    <item
        android:id="@+id/video_normal"
        android:drawable="@drawable/meizhi_video_button"
        android:duration="200"/>
    <item
        android:id="@+id/video_ing"
        android:drawable="@drawable/meizhi_camera_button_stop"
        android:duration="200"/>


</animation-list>
